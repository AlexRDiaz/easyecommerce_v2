import 'package:flutter/material.dart';

class OrdersHistoryTransportControllers {
  TextEditingController searchController = TextEditingController(
      text:
          "${DateTime.now().month}/${DateTime.now().day}/${DateTime.now().year}");
}
